/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HXUI_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLAIM_REGISTRATION_DEPOSIT_DISCRIMINATOR = new Uint8Array([
  204, 150, 17, 4, 148, 160, 151, 176,
]);

export function getClaimRegistrationDepositDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_REGISTRATION_DEPOSIT_DISCRIMINATOR,
  );
}

export type ClaimRegistrationDepositInstruction<
  TProgram extends string = typeof HXUI_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountFreeMintTracker extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountFreeMintTracker extends string
        ? WritableAccount<TAccountFreeMintTracker>
        : TAccountFreeMintTracker,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimRegistrationDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClaimRegistrationDepositInstructionDataArgs = {};

export function getClaimRegistrationDepositInstructionDataEncoder(): FixedSizeEncoder<ClaimRegistrationDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLAIM_REGISTRATION_DEPOSIT_DISCRIMINATOR,
    }),
  );
}

export function getClaimRegistrationDepositInstructionDataDecoder(): FixedSizeDecoder<ClaimRegistrationDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimRegistrationDepositInstructionDataCodec(): FixedSizeCodec<
  ClaimRegistrationDepositInstructionDataArgs,
  ClaimRegistrationDepositInstructionData
> {
  return combineCodec(
    getClaimRegistrationDepositInstructionDataEncoder(),
    getClaimRegistrationDepositInstructionDataDecoder(),
  );
}

export type ClaimRegistrationDepositAsyncInput<
  TAccountOwner extends string = string,
  TAccountFreeMintTracker extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  freeMintTracker?: Address<TAccountFreeMintTracker>;
};

export async function getClaimRegistrationDepositInstructionAsync<
  TAccountOwner extends string,
  TAccountFreeMintTracker extends string,
  TProgramAddress extends Address = typeof HXUI_PROGRAM_ADDRESS,
>(
  input: ClaimRegistrationDepositAsyncInput<
    TAccountOwner,
    TAccountFreeMintTracker
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimRegistrationDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountFreeMintTracker
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HXUI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    freeMintTracker: { value: input.freeMintTracker ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.freeMintTracker.value) {
    accounts.freeMintTracker.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            102, 114, 101, 101, 95, 109, 105, 110, 116, 95, 116, 114, 97, 99,
            107, 101, 114,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.freeMintTracker),
    ],
    data: getClaimRegistrationDepositInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimRegistrationDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountFreeMintTracker
  >);
}

export type ClaimRegistrationDepositInput<
  TAccountOwner extends string = string,
  TAccountFreeMintTracker extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  freeMintTracker: Address<TAccountFreeMintTracker>;
};

export function getClaimRegistrationDepositInstruction<
  TAccountOwner extends string,
  TAccountFreeMintTracker extends string,
  TProgramAddress extends Address = typeof HXUI_PROGRAM_ADDRESS,
>(
  input: ClaimRegistrationDepositInput<TAccountOwner, TAccountFreeMintTracker>,
  config?: { programAddress?: TProgramAddress },
): ClaimRegistrationDepositInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountFreeMintTracker
> {
  // Program address.
  const programAddress = config?.programAddress ?? HXUI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    freeMintTracker: { value: input.freeMintTracker ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.freeMintTracker),
    ],
    data: getClaimRegistrationDepositInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimRegistrationDepositInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountFreeMintTracker
  >);
}

export type ParsedClaimRegistrationDepositInstruction<
  TProgram extends string = typeof HXUI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    freeMintTracker: TAccountMetas[1];
  };
  data: ClaimRegistrationDepositInstructionData;
};

export function parseClaimRegistrationDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimRegistrationDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), freeMintTracker: getNextAccount() },
    data: getClaimRegistrationDepositInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
